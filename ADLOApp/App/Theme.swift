import SwiftUI

/// Placeholder brand palette — swap for ADLO's official brand colors/assets.
enum Theme {
    static let navy = Color(red: 0.05, green: 0.13, blue: 0.27)
    static let gold = Color(red: 0.78, green: 0.63, blue: 0.24)
    static let accent = gold
    static let background = Color(uiColor: .systemGroupedBackground)
}
